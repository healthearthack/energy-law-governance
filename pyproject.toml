[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "energy-law-governance"
version = "2.4.0"
description = "Machine-Readable Statutory Compliance & Critical Minerals Law Engine"
readme = "README.md"
authors = [{ name = "Doctoral Candidate healthearthack", email = "admin@thepolka.cloud" }]
license = { text = "MIT" }
dependencies = [
    "requests>=2.31.0",
    "pandas>=2.0.0"
]
